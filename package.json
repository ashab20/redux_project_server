{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Json server for lws qtk final module tasks",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "Ashab Uddin",
  "license": "ISC",
  "dependencies": {
    "json-server-auth": "^2.1.0",
    "nodemon": "^2.0.20"
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  }
}
